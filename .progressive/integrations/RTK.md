# RTK

Capability: compact shell/test/build/git output. Use only when supported filtering preserves command semantics and diagnostics; fall back to raw output when debugging ambiguous failures. Global hooks/command rewriting require explicit approval.

Official source: `https://github.com/rtk-ai/rtk`. Verify current official install/init instructions before enabling hooks.
