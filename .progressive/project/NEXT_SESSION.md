# Next Session

> Volatile hot context. Overwrite this file on each meaningful handoff. Durable phase history belongs in completed phase `Completion Record`s.

Outcome: IN PROGRESS

## Current phase
Phase 00 — Monorepo, Scene Schema & Fixture-Driven Figma Renderer — `.progressive/phases/00-scene-schema-renderer.md` (in progress — Tasks 1–5 done)

## Completed this session
- Completed Phase 00 Task 5: `packages/figma-renderer` — rectangle + ellipse render.
  - Added `packages/figma-renderer/src/shape-renderer.ts` (`renderRectangle`/`renderEllipse`, exported from `index.ts`) and `packages/figma-renderer/src/paint.ts` (`buildFillPaints`/`buildStrokePaints`/cap+join maps), and rewired `scene-renderer.ts`'s per-node loop into a `renderNode(frame, node)` switch dispatching `rectangle`/`ellipse` to the new renderers, falling through to the existing `unimplementedDiagnostic` for every other type (text/gradient/vector/image/group land in Tasks 6–10 by extending this same switch).
  - `applyShapeCommon`/`applyTransform`: `figma.createRectangle()`/`createEllipse()`, `resize(width,height)`, `x`/`y`/`rotation` set directly from `transform` (Figma's rotation pivots around the un-rotated top-left corner and leaves `x`/`y` invariant — Figma's own Design-panel/CSS-export "Position" display for a *rotated* node shows the rotated bounding box instead, which reads as a mismatch at a glance; verified by hand that the displayed value is exactly the expected AABB math, confirming the underlying node position is correct), `opacity` direct, `cornerRadius` (rectangle only, `?? 0`).
  - Fills: solid only (`buildFillPaints` in `paint.ts`) — non-solid (`linear-gradient`/`radial-gradient`) fills are skipped and surfaced as a `warning` Diagnostic (`gradient-fill-not-implemented`) rather than approximated, since gradient support is Task 7.
  - Strokes: `buildStrokePaints` maps every `Stroke` with a `color` to a `SolidPaint`; since Figma only has one aggregate `strokeWeight`/`dashPattern`/`strokeCap`/`strokeJoin` per node (not per-paint), those four are taken from the *first* stroke only — a Figma API limitation, not a schema gap.
  - Found and fixed an unrelated pre-existing blocker while doing the required manual Figma verification: the plugin threw `TypeError: BigInt is not a function` on any run under Figma's default "developer VM" (QuickJS-based dev sandbox, which lacks `BigInt` — a known Figma dev-VM limitation, likely hit via Zod v4 internals). Not a code bug; documented as a local Figma dev-environment setting (`Plugins → Development → Use developer VM` must be OFF while developing this plugin) rather than a code workaround, since production/published-plugin execution uses the full JS engine and doesn't hit this at all.
  - Manually verified all three existing fixtures in Figma desktop (imported `apps/figma-plugin/manifest.json` into a scratch draft file, since deleted): `fixtures/basic/rectangle.json` produced native `RectangleNode`s with position/size/opacity/fill/stroke/cornerRadius exactly matching the fixture, including the rotated one (Figma's UI shows the rotated bounding box for a rotated node's "Position", not raw x/y — verified by hand that the AABB math accounts for the full difference, so the underlying node position is correct); `fixtures/gradients/linear.json` correctly produced two rectangles with strokes but no fill plus two `gradient-fill-not-implemented` warning diagnostics; `fixtures/errors/unsupported-node.json` still isolates its unsupported sibling correctly (no dispatch regression).
- Completed Phase 00 Task 4: `apps/figma-plugin` shell.
  - `manifest.json` (`editorType: ["figma"]`, `networkAccess.allowedDomains: ["none"]` since no transport exists yet, `id: "slides2figma-dev-local"` placeholder for unregistered local dev).
  - `packages/figma-renderer/src/scene-renderer.ts` + `index.ts`: a minimal bootstrap `renderScene(scene)` that creates the root Frame (name `Google Slides / <slideId|title|presentationId>`, sized to `scene.canvas`), writes `slides2figma` plugin metadata (§37: `schemaVersion`/`presentationId`/`slideId`/`importedAt`/`source`), sorts `scene.nodes` by `zIndex`, selects + scrolls the frame into view. Per-node-type rendering is intentionally not implemented yet — every node currently produces an `info` Diagnostic (`renderer-not-implemented`) instead of a Figma node; Tasks 5–10 replace this loop with real dispatch per node type (rectangle/ellipse/text/gradient/vector/image/group).
  - `apps/figma-plugin/src/plugin/main.ts`: `figma.showUI` + `onmessage` handler that validates an incoming `{ type: 'render-fixture', scene }` message via `SceneSchema.safeParse` (invalid scene → `figma.notify(error)` + `render-error` postMessage, no crash — satisfies the phase's "malformed fixture rejected by schema validation" negative case), then calls `renderScene` and posts `{ type: 'render-result', diagnostics }` back to the UI.
  - `apps/figma-plugin/src/ui/{index.html,main.ts,fixtures.ts}`: plain HTML/TS (no React — deviates from Technical Spec §34's `ui/App.tsx` naming; kept minimal per Architecture.md's "React optional" note and AGENTS.md's anti-speculative-abstraction rule) — a `<select>` populated from the 4 existing `fixtures/` JSON files (imported directly at build time via `resolveJsonModule`, not read at runtime — the plugin iframe has no filesystem access) and a Render button that `postMessage`s the selected fixture's JSON to the plugin thread; a status area renders returned diagnostics.
  - Split `tsconfig.json` (plugin thread: `@figma/plugin-typings`, no DOM lib) from `src/ui/tsconfig.json` (DOM lib, no figma typings) — avoids the well-known `TextNode`/other symbol collision between `@figma/plugin-typings` and DOM lib when both are in one compile.
  - `apps/figma-plugin/scripts/build.mjs`: esbuild-based dev build script. Bundles `plugin/main.ts` → `dist/plugin/main.js` (iife); bundles `ui/main.ts` and inlines the JS into `dist/ui/index.html` (Figma requires the UI as one self-contained HTML file). `pnpm --filter @slides2figma/figma-plugin dev` runs both bundles under esbuild's watch/context API; `build` runs them once.
  - Added `@slides2figma/figma-renderer`/`@slides2figma/scene-schema` as `workspace:*` deps of `apps/figma-plugin`; `@slides2figma/scene-schema` as a dep and `@figma/plugin-typings` as a devDependency of `packages/figma-renderer`; `esbuild@^0.28.2` + `@figma/plugin-typings@^1.134.0` as devDependencies of `apps/figma-plugin`.
- Read `Slides_to_Figma_Technical_Spec_v0.1.md` in full and initialized Progressive Context project state from it.
- Wrote `PROJECT_BRIEF.md`, `ARCHITECTURE.md`, `ROADMAP.md` (6 coarse phases), and detailed `Phase 00` file.
- Added `CONTEXT_MANIFEST.json` hints pointing back to the source Technical Spec sections.
- Classified project Complexity: L, Risk: Medium.
- User confirmed the Phase 00/01 sequencing resolution (renderer-first, Phase 00 stays before extraction research). Recorded in `.progressive/phases/00-scene-schema-renderer.md` and `ROADMAP.md`.
- Completed Phase 00 Task 1: pnpm workspace monorepo scaffold — root `package.json`/`pnpm-workspace.yaml`/`tsconfig.base.json`/`vitest.config.ts`/`.gitignore`, plus `package.json`+`tsconfig.json` for `apps/figma-plugin`, `packages/scene-schema`, `packages/figma-renderer`, `packages/shared`, and an empty `fixtures/` dir.
- Completed Phase 00 Task 2: `packages/scene-schema` — TypeScript types + Zod (v4) schema for `Scene`, `SceneNodeBase`, `Transform2D`, `Fill` (solid/linear-gradient/radial-gradient), `Stroke`, `Diagnostic`, `Asset`, `TextSceneNode` (+`TextRun`, `ParagraphStyle`), `RectangleNode`, `EllipseNode`, `VectorSceneNode`, `ImageSceneNode`, `GroupNode`, `UnsupportedNode` (Technical Spec §10–24), plus 55 Vitest unit tests covering valid/invalid parsing per schema, including recursive `GroupNode` nesting and malformed-scene rejection.
- Completed Phase 00 Task 3: hand-authored fixture JSON — `fixtures/basic/rectangle.json` (plain + rounded rectangle, mirrors Slide 01), `fixtures/text/mixed-text.json` (one `TextSceneNode`, 5 runs mixing font family/size/weight/color/bold/italic/letter-spacing, 2 paragraphs with different alignment/line-height, mirrors Slide 04), `fixtures/gradients/linear.json` (2-stop horizontal + 4-stop diagonal linear gradients covering a transparent stop, opacity, and a stroke, mirrors Slide 03), and `fixtures/errors/unsupported-node.json` (a valid rectangle sibling next to an `unsupported` chart node with a `warning` Diagnostic, for the per-child error-isolation test, mirrors Slide 12). Added `packages/scene-schema/src/fixtures.test.ts` to assert every fixture parses via `SceneSchema.safeParse` and that the unsupported fixture keeps its valid sibling and warning diagnostic.

## Verification evidence
- `pnpm install` — resolves all 5 workspace projects cleanly; added `zod@^4.4.3` to `packages/scene-schema`.
- `pnpm exec tsc -p packages/scene-schema/tsconfig.json --noEmit` → no errors.
- `pnpm -w test` → 15 test files, 59 tests passed (55 prior + 4 new fixture-validation tests).
- `pnpm --filter @slides2figma/scene-schema build` → compiles cleanly to `dist/`; `tsconfig.json` now excludes `src/**/*.test.ts` so test files don't ship in the built package.
- Task 4: `pnpm --filter @slides2figma/figma-renderer build` (tsc, `@figma/plugin-typings` resolved) → no errors. `pnpm --filter @slides2figma/figma-plugin typecheck` (both `tsconfig.json` and `src/ui/tsconfig.json`) → no errors. `node apps/figma-plugin/scripts/build.mjs` → `dist/plugin/main.js` (608KB, zod+scene-schema bundled) and `dist/ui/index.html` (9.4KB, fixtures inlined — spot-checked `rect-plain-1`/`Rectangle` from `fixtures/basic/rectangle.json` present in the built HTML). `pnpm -w test` re-run after all Task 4 changes → still 15 files/59 tests passing, no regressions.
- Task 5: `pnpm --filter @slides2figma/figma-renderer build` and `node apps/figma-plugin/scripts/build.mjs` → no errors, `dist/plugin/main.js` rebuilt (596.8KB). `pnpm -w test` → still 15 files/59 tests, no regressions.
- Task 5: manually loaded the plugin in the real Figma desktop app (first time this shell has been exercised there — Task 4 left this outstanding). Confirmed: `fixtures/basic/rectangle.json` produces two native `RectangleNode`s with position/size/rotation/opacity/fill/stroke/cornerRadius all matching the fixture (rotated one verified via AABB hand-math against Figma's rotated-bounding-box position display); `fixtures/gradients/linear.json` produces rectangles with strokes-only (no fill) plus the expected `gradient-fill-not-implemented` warning diagnostics; `fixtures/errors/unsupported-node.json` still isolates correctly (no regression in the existing diagnostic-fallback dispatch).

## Blockers / uncertainty
- None currently blocking. Tooling profile still not selected/bootstrapped (`TOOLING_STATUS.json` is still `not_selected`) — deferred, not required to keep making Phase 00 progress.
- Figma desktop's "Use developer VM" setting (`Plugins → Development → Use developer VM`) must stay OFF for this plugin during local development — its QuickJS-based dev sandbox lacks `BigInt` and the plugin throws immediately on run otherwise (`TypeError: BigInt is not a function`, first hit while doing Task 5's required manual verification). Not a code bug — a local Figma dev-environment setting; production/published-plugin execution isn't affected.

## Important decisions
- `Fill` union scoped to `solid | linear-gradient | radial-gradient` for this phase (angular gradients and image fills are explicitly out of scope per the phase file, even though the Technical Spec §12 union lists them — deferred to Phase 03).
- `SceneNode` union scoped to the phase file's explicit type list (`text | rectangle | ellipse | vector | image | group | unsupported`); `FrameNode`, `LineNode`, `TableNode` from the Technical Spec §11 full union are out of scope for Phase 00.
- `GroupNode`/`SceneNode` are mutually recursive, which Zod can't `z.infer`; hand-wrote `SceneNodeBase`/`GroupNode`/`SceneNode` TypeScript interfaces in `scene-node.ts` and typed the schema against them, while every other node/type uses `z.infer` directly to avoid duplicating type definitions.
- Colocated `*.test.ts` files next to their source modules (matches `vitest.config.ts`'s existing glob); added `src/test-fixtures.ts` (non-test helper, not exported from `index.ts`) for a shared minimal-valid `SceneNodeBase` fixture used across node tests.
- Fixture folder taxonomy is by feature (`fixtures/basic/`, `fixtures/text/`, `fixtures/gradients/`, `fixtures/errors/`), one folder per fixture category, matching the phase file's own fixture path naming; `errors/` holds the unsupported-node/error-isolation fixture since it didn't fit the other three categories.
- Each fixture's `schemaVersion`/`source`/`canvas` fields follow the exact convention already established in `scene.test.ts`'s `validScene()` helper (`"0.1.0"`, `app: "google-slides"`, `1920×1080` canvas at `unit: "source"`) rather than inventing new values, and each fixture's `source.slideId`/`title` cite the Technical Spec §50 slide it mirrors (Slide 01/03/04/12).
- Added `packages/scene-schema/src/fixtures.test.ts` (reads fixture JSON from repo-root `fixtures/` via `fs.readFileSync`, not a bundler JSON import) so `pnpm -w test` gives automated evidence every fixture actually conforms to `SceneSchema`, not just visual inspection.
- Task 4: kept `packages/figma-renderer`'s node-creation code strictly to the root Frame + metadata + diagnostics-only skeleton described in §36/§37, deliberately not building any per-node-type visual placeholder (e.g. a gray box per unsupported child) — that behavior belongs to Task 10 ("diagnostics reporter + per-child error isolation"), not the Task 4 shell; building it now would have preempted/duplicated that task's scope.
- Task 4: UI is plain HTML + vanilla TS, not React/`App.tsx` as literally named in Technical Spec §34's module tree — Architecture.md already marks React as "optional, for plugin/extension UI", and a fixture-picker dropdown + button doesn't need a component framework. `renderer/*`/`fonts/resolver.ts` from §34's tree also aren't under `apps/figma-plugin/src/` — Architecture.md already resolved that split during Task 1 by giving `packages/figma-renderer` its own workspace package for node-creation logic (`fonts/resolver.ts` stays under the plugin app per Architecture.md's "Sources of truth" table, but isn't needed until Task 6).
- Task 4: split `apps/figma-plugin/tsconfig.json` (plugin thread) from `src/ui/tsconfig.json` (UI/browser thread) rather than one shared config, because `@figma/plugin-typings`' `TextNode` (and other) declarations collide with the DOM lib's own `TextNode` if both are included in one `tsc` program — a known issue with Figma plugin templates. Each is `noEmit: true` since esbuild (not `tsc`) does the actual bundling/emit for this app; `tsc` is used purely as an explicit `typecheck` script.
- Task 4: `manifest.json` sets `networkAccess.allowedDomains: ["none"]` rather than pre-wiring the `localhost:4317` dev relay domain from Architecture.md, since no transport code exists yet in this phase (Phase 04) — network access will be loosened when the relay client is actually added, not preemptively.
- Task 4: `manifest.json`'s `"id"` is a placeholder string (`"slides2figma-dev-local"`) since this plugin isn't registered with Figma yet; local/dev "Import plugin from manifest…" doesn't validate it against a registry. Revisit if/when the plugin is ever published.
- Task 5: `x`/`y`/`rotation` are set directly from `transform` in that order with no matrix math (`figmaNode.x = transform.x; figmaNode.y = transform.y; figmaNode.rotation = transform.rotation`) — Figma's rotation setter pivots around the un-rotated top-left corner and leaves the underlying `x`/`y` invariant per the plugin API docs. Verified this is correct (not a bug) by hand-checking that Figma's Design-panel/CSS-export "Position" display for the *rotated* fixture rectangle — which shows a different value than the fixture's `x`/`y` — is exactly the rotated bounding box's top-left (AABB math), a UI display convention, not the actual node position.
- Task 5: non-solid fills (`linear-gradient`/`radial-gradient`) are skipped with a `warning` Diagnostic (`gradient-fill-not-implemented`) rather than approximated as a solid color, so the gap stays visible instead of silently faked; gradient support is Task 7.
- Task 5: strokes' `strokeWeight`/`dashPattern`/`strokeCap`/`strokeJoin` are taken from only the *first* `Stroke` in a node's `strokes[]`, since Figma has one aggregate value per node for each of those (not per-paint) — every stroke's *color* still contributes its own `SolidPaint` to `figmaNode.strokes[]`.
- Task 5: discovered Figma desktop's default "Use developer VM" (QuickJS dev sandbox) setting breaks this plugin entirely (`BigInt is not a function`) — documented as a required local dev-environment setting (turn it off) rather than working around it in code, since it doesn't affect production/published-plugin execution.

## Next action
Phase 00 Task 6: `packages/figma-renderer` — text render (font resolver, per-run styling, paragraph styling). Replace the `unimplementedDiagnostic` branch for `type: 'text'` nodes with a real `figma.createText()` call per Technical Spec §16–17's documented order: `createText` → collect the distinct fonts used across all `TextRun`s → `loadFontAsync` each (required before any text/style mutation) → set `characters` from the concatenated run text → apply per-run styling (font family/size/weight/color/bold/italic/letter-spacing) by UTF-16 range via `setRangeFontName`/`setRangeFontSize`/`setRangeFills`/etc., and paragraph-level styling (alignment/line-height) from `ParagraphStyle`. A font resolver/fallback module is in scope per the phase file ("`text-renderer` (with font resolver)") — `fonts/resolver.ts` was already flagged in Task 4 as belonging under `apps/figma-plugin/src/` per Architecture.md's "Sources of truth" table, not `packages/figma-renderer`, so place it there and have the renderer call into it. Verify against `fixtures/text/mixed-text.json` in Figma desktop (same manifest; remember "Use developer VM" must be OFF) — acceptance criterion: "one editable Figma `TextNode` with correct per-range font/size/weight/color, and the text can be edited by hand after import." Keep `pnpm -w test` green and extend the same `renderNode` switch in `scene-renderer.ts` rather than restructuring it.

## NEXT SESSION PROMPT
```text
Continue Phase 00 Task 6: packages/figma-renderer — text render (font resolver,
per-run styling, paragraph styling). Replace the unimplementedDiagnostic branch in
scene-renderer.ts for type: 'text' nodes with a real figma.createText() call following
Technical Spec §16-17's order: createText -> collect fonts used across TextRuns ->
loadFontAsync each -> set characters -> apply per-run styling by UTF-16 range -> apply
paragraph styling. Add a font resolver/fallback module under apps/figma-plugin/src/
(per Architecture.md's "Sources of truth" table, not packages/figma-renderer) and have
the renderer call into it. Extend the same renderNode switch in scene-renderer.ts rather
than restructuring it (rectangle/ellipse dispatch from Task 5 must keep working). Verify
against fixtures/text/mixed-text.json in the Figma desktop app (manifest at
apps/figma-plugin/manifest.json) -- NOTE: Plugins -> Development -> "Use developer VM"
must be OFF in Figma desktop or the plugin throws "BigInt is not a function" immediately
(QuickJS dev-sandbox limitation, unrelated to this codebase). Keep pnpm -w test green.
Per .progressive/phases/00-scene-schema-renderer.md acceptance criteria.

Read the active instruction layers, recover project state from the Default Read Set,
verify the Roadmap marker, and continue the next action autonomously. Do not reread
full completed phases or chat history unless evidence requires it.
```
