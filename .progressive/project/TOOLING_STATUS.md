# Tooling Status

Canonical machine state: `TOOLING_STATUS.json`.

Status: NOT CHECKED

Use `python3 .progressive/tools/tooling_status.py --report` for a current human-readable view. Agent-environment capabilities that cannot be safely detected from the local shell must be verified by the active agent and persisted in the JSON cache.
