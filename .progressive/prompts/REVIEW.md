# Review

Load project constraints relevant to the diff, invoke `code-review`, verify current-phase
acceptance claims against actual code/tests, invoke security workflow for sensitive
boundaries, and separate blocking defects from reasonable follow-ups.
